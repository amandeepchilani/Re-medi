import { PatientAppointmentReport } from './patient-appointment-report';

describe('PatientAppointmentReport', () => {
  it('should create an instance', () => {
    expect(new PatientAppointmentReport()).toBeTruthy();
  });
});
