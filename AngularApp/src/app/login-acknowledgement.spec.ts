import { LoginAcknowledgement } from './login-acknowledgement';

describe('LoginAcknowledgement', () => {
  it('should create an instance', () => {
    expect(new LoginAcknowledgement()).toBeTruthy();
  });
});
